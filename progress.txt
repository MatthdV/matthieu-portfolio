2026-03-18: Phase 1 — Supprimer l'ancien design. Stripped all pages to minimal placeholders, removed old components (Navbar, Footer, ThemeProvider, ContactForm, NewsletterForm, ProjectCard), reset globals.css and tailwind.config.ts for marker palette. Kept i18n setup, messages, data files, FadeIn, middleware, and config intact. Moved Project type from deleted ProjectCard into data/projects.ts.
2026-03-18: Phase 1 — Configurer les fonts Space Grotesk + JetBrains Mono. Added next/font/google imports in locale layout with CSS variables --font-space-grotesk and --font-jetbrains-mono. Applied variables to html element, set font-sans as default body font. Tailwind config already maps font-sans → Space Grotesk and font-mono → JetBrains Mono.
2026-03-18: Phase 1 — Mettre à jour les couleurs Tailwind (palette marker). Defined all color tokens as CSS custom properties in :root (background #0a0a0a, foreground #f5f5f0, muted #888888, border #222222, marker-blue #1A65FF, marker-yellow #FFCD2E, marker-red #FE342C). Updated tailwind.config.ts to reference CSS vars instead of hardcoded hex values. Body uses var() for color and background.
2026-03-18: Phase 1 — Ajouter le grain overlay CSS sur le body. Added body::after pseudo-element with SVG feTurbulence noise filter (fractalNoise, baseFrequency 0.65, 3 octaves). Fixed position, full viewport coverage, z-index 9999, pointer-events none, opacity 0.04. Gives the page a subtle paper/zine grain texture.
2026-03-18: Phase 1 — Créer le composant Navbar v2. Built Navbar (client component) with logo "matthieu." (blue dot), uppercase mono nav links (home, projets, services, blog, contact), and LanguageSwitcher (FR/EN/ES). Fixed position with backdrop blur, border-bottom. Mobile hamburger menu with animated bars. All translations passed as props from server layout. Added pt-16 spacer on body content to account for fixed navbar.
2026-03-18: Phase 1 — Créer le composant Footer v2 (minimal). Built Footer as a server component with logo "matthieu." (blue dot), tagline in mono, nav links (uppercase mono matching Navbar style), and copyright line. Three-column layout on desktop (logo+tagline | nav links | copyright), stacked on mobile. Translations passed as props from layout. Added to locale layout below main content.
2026-03-18: Phase 2 — Hero section asymétrique avec stats et CTAs. Built HeroSection server component with asymmetric 2-column grid layout. Left column: availability tag with blue marker line, massive H1 (clamp 3rem-5.5rem) with yellow marker underline on accent text, grey description, 3 stats in JetBrains Mono, 2 CTAs (blue primary with yellow box-shadow hover, ghost with border hover). Right column: hero-illustration.png via next/image, decorative blue blur blob, colored marker strokes. Added translation keys (heroTag, heroDescription, stats) to all 3 locale files. All translations passed as props from server page.
2026-03-18: Phase 2 — Intégrer hero-illustration.png. Already implemented as part of HeroSection build — hero-illustration.png exists in public/images/ and is rendered via next/image with priority loading, fill layout, object-contain, proper alt text. Decorative elements (blue blur blob, marker strokes) surround the illustration.
2026-03-18: Phase 2 — Créer les composants marker-underline et marker-circle. Built two reusable server components: MarkerUnderline (skewed color bar beneath text via ::after-style absolute span, supports yellow/blue/red color prop) and MarkerCircle (SVG dashed ellipse around content, hand-drawn aesthetic with slight rotation, supports yellow/blue/red). Refactored HeroSection to use MarkerUnderline instead of inline marker styling. Both components accept children + optional color and className props.
2026-03-18: Phase 2 — Section services en bento grid avec illustrations marker. Built ServicesSection server component with bento grid layout (3 columns). Section label mono with blue marker line, title with red MarkerUnderline on accent word. 4 service cards: Agents IA (featured, spans 2 cols) with icon-agents.png, Automation with icon-automation.png, Audit IA with icon-audit.png (price in blue), Retainer with icon-retainer.png. Each card has subtle border, hover → blue border + translateY(-4px). Prices in yellow (JetBrains Mono), audit price in blue. Added translation keys (servicesSectionLabel, servicesBentoTitle/Accent, service prices, service4 title/desc/price) to all 3 locale files. Wired into homepage page.tsx below HeroSection.
2026-03-18: Phase 2 — Section projets en liste horizontale style terminal. Built ProjectsSection server component with terminal-style list layout. Section label mono "02 — Projets" with blue marker line, title with yellow MarkerCircle on accent word ("prod."). 7 project rows in a grid (number mono | title bold | tags mono | arrow →). Hover: blue tinted background + arrow translates + turns blue. Bottom link to GitHub. Added translation keys (projectsSectionLabel, projectsTitle, projectsTitleAccent, projectsViewAll) to all 3 locale files. Wired into homepage page.tsx below ServicesSection with project data from Projects translations.
2026-03-18: Phase 2 — Section newsletter avec formulaire inline. Built NewsletterSection client component with section label mono "03 — Newsletter" with blue marker line, title with blue MarkerUnderline on accent word, grey description, and inline form (mono input + blue button flush, no gap). Hover on button: translate + yellow box-shadow. icon-newsletter.png displayed via next/image. Simple submitted state shows "OK" confirmation. Added translation keys (newsletterSectionLabel, newsletterTitle, newsletterTitleAccent) to all 3 locale files. Translations passed as props from server page. Wired into homepage page.tsx below ProjectsSection.
2026-03-18: Phase 3 — Page /projets avec détails de chaque projet. Built full projects page with: section label mono + blue marker line, H1 with clamp sizing, description in muted text. 7 project cards with number prefix, title (first project gets yellow MarkerUnderline), tech tags in bordered pills, description, 3-column grid for Problem (red marker label), Stack (blue marker label with tinted pills), and Results (yellow arrows in mono). Each card has hover: blue border + translateY(-4px). GitHub link per project. Bottom CTA section with link to /contact and blue button with yellow box-shadow hover. Uses getLocalizedProjects for i18n. All translations from existing ProjectsPage + Projects keys. Server component, no useTranslations in client code.
2026-03-18: Phase 3 — Page /services avec cartes et pricing. Built full services page with: header section (section label mono + blue marker line, H1 clamp sizing, muted description), 2-column grid of 4 service cards. Each card has marker icon (PNG via next/image), service name (first card gets blue MarkerUnderline), price in mono (yellow or blue), duration tag, description, 4-feature list with blue arrow markers, and ghost CTA button with blue hover shadow. Service 3 (Agent IA) has red "Populaire" badge. Bottom CTA section with description and blue primary button (yellow box-shadow hover). All translations from existing ServicesPage keys (3 locales). Server component using useTranslations.
